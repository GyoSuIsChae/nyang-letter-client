import { DefaultTheme } from 'styled-components';

import { FONT_WEIGHTS, COLORS, Z_INDEX_LAYER } from '@constants/theme';

const theme: DefaultTheme = {
  fontWeights: FONT_WEIGHTS,
  colors: COLORS,
  zIndexLayer: Z_INDEX_LAYER,
};

export default theme;
