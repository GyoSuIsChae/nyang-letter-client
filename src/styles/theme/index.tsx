import { DefaultTheme } from 'styled-components';

import {
  FONT_SIZES,
  FONT_WEIGHTS,
  COLORS,
  Z_INDEX_LAYER,
} from '@constants/theme';

const theme: DefaultTheme = {
  fontSizes: FONT_SIZES,
  fontWeights: FONT_WEIGHTS,
  colors: COLORS,
  zIndexLayer: Z_INDEX_LAYER,
};

export default theme;
